{
  "dependencies": {
    "bootstrap": "^5.3.8"
  },
  "devDependencies": {
    "sass": "^1.93.3"
  },
  "scripts": {
    "sass": "sass --quiet-deps --watch src/scss:dist/css",
    "build": "sass --quiet-deps src/scss:dist/css",
    "watch": "sass --quiet-deps --watch src/scss:dist/css & live-server"
  }
}
